
# Shifu Trading Engine Replay

An event-driven Streamlit application for replaying an out-of-sample trading run built from DeepLOB signals and a Deep Learning Sharpe (DLS) portfolio allocation layer.

This project is **not** a static CSV dashboard. It rebuilds the trading process as a replayable state machine:

```text
DeepLOB probabilities
    -> signal score and confidence
    -> entry / quality filters
    -> DLS target weights
    -> submitted orders
    -> executed trades
    -> end-of-day holdings
    -> portfolio and model comparison metrics
```

The app is designed for inspecting *why* a trading decision happened on each out-of-sample day, not only for viewing final performance.

---

## 1. Main capabilities

- Interactive day-by-day replay of the trading engine.
- Manual replay with previous / next buttons and a day slider.
- Optional auto-play with safe refresh intervals.
- Fast playback mode that skips heavy plots while the replay is running.
- Same-day DeepLOB three-class signal inspection: `P(down)`, `P(flat)`, `P(up)`.
- Asset-level decision book linking `P(down)`, `P(flat)`, `P(up)`, filters, previous weights, DLS target weights, submitted orders, filled executions, and final EOD holdings.
- Execution reconciliation table showing submitted buy/sell percentages versus filled buy/sell percentages for every asset.
- Daily comparison between:
  - `Base DeepLOB`
  - `DeepLOB + DLS`
- Final model comparison cockpit with return, Sharpe, drawdown, cost, and order divergence.
- Training diagnostics for DLS training history and optional seed-search analysis.

---

## 2. Repository layout

Recommended GitHub structure:

```text
.
├── streamlit_app.py
├── requirements.txt
├── README.md
└── data/
    ├── T001_comparison_original_deeplob_exact_vs_dls.csv
    ├── T001_dls_holding_snapshot_audit.csv
    ├── T001_dls_trade_audit.csv
    ├── T001_dls_weight_debug_shifted_tplus2_conf_filter.csv
    ├── T001_dls_weight_step_audit.csv
    ├── T001_dls_weights_long_shifted_tplus2.csv
    ├── T001_original_deeplob_exact_daily_log.csv
    ├── T001_original_deeplob_exact_raw_oos_signals.csv
    ├── T001_oos_original_deeplob_exact_sell_close.csv
    ├── T001_oos_shifu_dls_colab_sell_open.csv
    ├── shifu_dls_seed_search_summary.csv
    └── shifu_dls_training_history_oos.csv
```

The app expects all CSV files to be inside a folder called `data/` next to `streamlit_app.py`:

```python
DATA_DIR = Path(__file__).resolve().parent / "data"
```

---

## 3. Installation

Create a virtual environment if desired:

```bash
python -m venv .venv
```

Activate it:

```bash
# Windows PowerShell
.venv\Scripts\Activate.ps1

# macOS / Linux
source .venv/bin/activate
```

Install dependencies:

```bash
pip install -r requirements.txt
```

A minimal `requirements.txt` should include:

```text
streamlit
pandas
numpy
plotly
streamlit-autorefresh
```

`streamlit-autorefresh` is optional. Manual replay still works without it, but auto-play needs it.

---

## 4. Running the app

```bash
streamlit run streamlit_app.py
```

If you previously ran an older version and Streamlit behaves strangely, clear the cache:

```bash
streamlit cache clear
```

Then restart the app.

---

## 5. Data files used by the app

| Internal key         | CSV file                                                 | Purpose                                                                                                |
| -------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `comparison`       | `T001_comparison_original_deeplob_exact_vs_dls.csv`    | Final model-level metrics for Base DeepLOB vs DeepLOB + DLS.                                           |
| `dls_holding`      | `T001_dls_holding_snapshot_audit.csv`                  | End-of-day holdings generated by the DLS strategy.                                                     |
| `dls_trade`        | `T001_dls_trade_audit.csv`                             | Filled DLS trades, including side, shares, execution price, turnover, and cost.                        |
| `dls_weight_debug` | `T001_dls_weight_debug_shifted_tplus2_conf_filter.csv` | Day-level optimizer/debug information such as target gross, filter thresholds, and target-name counts. |
| `dls_weight_step`  | `T001_dls_weight_step_audit.csv`                       | Main asset-level DLS decision table used to reconstruct the decision chain.                            |
| `dls_weights_long` | `T001_dls_weights_long_shifted_tplus2.csv`             | Long-format target weights by day and asset.                                                           |
| `base_daily_log`   | `T001_original_deeplob_exact_daily_log.csv`            | Daily portfolio value, cash, holdings, buys, sells, turnover, and costs for the Base DeepLOB strategy. |
| `raw_signals`      | `T001_original_deeplob_exact_raw_oos_signals.csv`      | Raw DeepLOB out-of-sample signals and three-class probabilities.                                       |
| `base_submission`  | `T001_oos_original_deeplob_exact_sell_close.csv`       | Base DeepLOB submitted buy/sell orders.                                                                |
| `dls_submission`   | `T001_oos_shifu_dls_colab_sell_open.csv`               | DeepLOB + DLS submitted buy/sell orders.                                                               |
| `seed_search`      | `shifu_dls_seed_search_summary.csv`                    | DLS seed-search summary metrics.                                                                       |
| `training_history` | `shifu_dls_training_history_oos.csv`                   | DLS training and validation history over epochs.                                                       |

---

## 6. How the app reconstructs the replay state

The app loads the CSVs, normalizes important numeric columns, and creates a unified daily replay table called `summary`.

At every replay step, the app builds an `EngineState` object:

```text
EngineState = {
    day,
    index,
    row,
    raw DeepLOB signals,
    DLS weight-step rows,
    DLS filled trades,
    DLS holdings,
    DLS weight/debug row,
    DLS submitted orders,
    Base DeepLOB submitted orders
}
```

This lets every dashboard view reference the same current replay day.

### Important derived metrics

| Metric                      | Formula / source                                                          | Meaning                                                                     |
| --------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `day_index`               | Numeric part of`trade_day_id`                                           | Used to sort replay days chronologically.                                   |
| `engine_target_gross`     | `target_gross`, fallback to sum of target weights, fallback to `0.95` | Target gross exposure used by the DLS engine.                               |
| `dls_equity_proxy`        | `eod_market_value / engine_target_gross`                                | Reconstructed DLS portfolio equity proxy.                                   |
| `dls_cash_proxy`          | `dls_equity_proxy - eod_market_value`                                   | Implied cash proxy under the reconstructed DLS equity assumption.           |
| `dls_return_proxy_%`      | `(dls_equity_proxy / first_valid_dls_equity_proxy - 1) * 100`           | Reconstructed cumulative DLS return path.                                   |
| `base_return_%`           | `(base_portfolio_value / first_base_portfolio_value - 1) * 100`         | Base DeepLOB cumulative return path.                                        |
| `dls_daily_return_%`      | `pct_change(dls_equity_proxy) * 100`                                    | DLS same-day return proxy.                                                  |
| `base_daily_return_%`     | `pct_change(base_portfolio_value) * 100`                                | Base DeepLOB same-day return.                                               |
| `executed_total_turnover` | DLS filled buy + sell turnover                                            | Total DLS traded notional for the replay day.                               |
| `base_total_turnover_day` | `base_buy_turnover + base_sell_turnover`                                | Total Base DeepLOB traded notional for the replay day.                      |
| `dls_cum_turnover`        | Cumulative sum of DLS turnover                                            | Cumulative DLS trading intensity.                                           |
| `base_cum_turnover`       | Cumulative sum of base turnover                                           | Cumulative Base DeepLOB trading intensity.                                  |
| `dls_cum_cost`            | Cumulative sum of DLS costs                                               | Cumulative DLS trading cost.                                                |
| `base_cum_cost`           | Cumulative sum of base costs                                              | Cumulative Base DeepLOB trading cost.                                       |
| `quality_pass_rate`       | `quality_candidates / entry_candidates`                                 | Fraction of entry candidates that pass quality filtering.                   |
| `target_utilization`      | `target_names / quality_candidates`                                     | Fraction of quality-passed candidates that receive positive target weights. |
| `execution_rate`          | `executed_assets / target_names`                                        | Fraction of targeted names that actually trade.                             |

---

## 7. Sidebar controls

The left sidebar is the control panel for the replay engine.

| Control                     | Meaning                                                                                                                                             |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Previous`                | Move one replay step backward.                                                                                                                      |
| `Next`                    | Move one replay step forward.                                                                                                                       |
| `Replay day` slider       | Jump directly to a selected replay index.                                                                                                           |
| `Play / Pause`            | Start or stop auto replay.                                                                                                                          |
| `Playback speed`          | Select refresh interval:`0.5x`, `1x`, `2x`, `5x`, or `10x`.                                                                               |
| `Loop playback`           | Restart from the first day when the final day is reached.                                                                                           |
| `Enable auto-play module` | Enables`streamlit-autorefresh`; keep off for fastest startup.                                                                                     |
| `Fast playback rendering` | When playing, render only lightweight state cards and event logs.                                                                                   |
| `Engine View`             | Chooses the current app view: Engine Replay, Decision Inspector, Execution Tape, Portfolio State, Daily Metrics, Model Comparison, or Training Lab. |

The app intentionally uses a single-view sidebar radio instead of heavy `st.tabs`, because Streamlit tabs render all tabs at once. This keeps the app faster and more reliable.

---

## 8. Global status strip

The top of the app always shows the current replay state.

| Status card           | Meaning                                                       |
| --------------------- | ------------------------------------------------------------- |
| `Engine Clock`      | Current`trade_day_id`, plus the related `signal_day_id`.  |
| `DLS Equity Proxy`  | Reconstructed DeepLOB + DLS equity value for the current day. |
| `Return Proxy`      | Cumulative return of the reconstructed DLS path.              |
| `Target Gross`      | Gross target exposure used by the DLS optimizer.              |
| `Executed Turnover` | DLS buy + sell notional executed on the current day.          |
| `EOD Holdings`      | Number of active holdings at end of day.                      |

Below that, the stage line shows the trading pipeline:

1. `Signal Bus`
2. `Entry Mask`
3. `Quality Filter`
4. `DLS Optimizer`
5. `Execution`
6. `Portfolio State`

### Trading Pipeline Stage Line

The stage line is a compact replay of the full trading engine for the selected out-of-sample day. It should be read from left to right as a state transition, not as a set of independent performance metrics.

```text
Signal Bus
    -> Entry Mask
    -> Quality Filter
    -> DLS Optimizer
    -> Execution
    -> Portfolio State
```

Each stage answers one question:

```text
What did DeepLOB predict?
    -> Which assets were tradable/eligible?
    -> Which signals were strong enough?
    -> What portfolio weights did DLS want?
    -> What orders were actually executed?
    -> What did the portfolio hold at the end of the day?
```

The notebook uses a shifted, no-lookahead convention for the DLS pipeline. A DeepLOB signal is generated on a signal/observation day `t`, using information available up to that day. The resulting decision is applied to the next trade day `t+1`. This is why the dashboard shows both a `signal_day_id` and a `trade_day_id`. The stage line therefore represents the journey from a prior-day model signal into a next-day trading decision.

---

#### 1. `Signal Bus`

`Signal Bus` is the raw DeepLOB prediction layer and represents the full DeepLOB signal universe for the selected replay day.

At this stage, no portfolio allocation has been made yet. The model has only produced a three-class probability vector for each asset:

```text
P(down), P(flat), P(up)
```

These probabilities come from the DeepLOB classification output:

* `P(down)` is the probability that the asset will move downward.
* `P(flat)` is the probability that the asset will remain approximately flat or have no meaningful directional move.
* `P(up)` is the probability that the asset will move upward.

The dashboard’s `Signal Bus` number shows the total number of assets for which DeepLOB produced a usable signal/probability vector on the selected replay day. In other words:

```text
Signal Bus = all raw DeepLOB signal rows available for the current day
```

The subtitle then breaks this full signal universe into directional groups:

```text
up signals   = assets where the DeepLOB signal is bullish
flat signals = assets where the DeepLOB signal is neutral
down signals = assets where the DeepLOB signal is bearish
```

Therefore, `Signal Bus` should be interpreted as the starting universe of model predictions, not as the number of buy orders and not as the final trading universe.

The notebook then derives two additional signal features from the three DeepLOB probabilities:

```text
signal_score = P(up) - P(down)

confidence = |signal_score| × (1 - P(flat))
```

The interpretation is:

| Feature        | Meaning                                                                                                                                               |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `P(up)`        | Bullish probability from DeepLOB.                                                                                                                     |
| `P(down)`      | Bearish probability from DeepLOB.                                                                                                                     |
| `P(flat)`      | Neutral/no-direction probability. High `P(flat)` means the model is less directionally confident.                                                     |
| `signal_score` | Directional strength. Positive values indicate more bullish than bearish probability; negative values indicate more bearish than bullish probability. |
| `confidence`   | Directional confidence. It becomes high only when the signal is directional and `P(flat)` is low.                                                     |

This stage answers:

```text
What did DeepLOB predict before any portfolio, tradability, or execution rule was applied?
```

A large `Signal Bus` value means many assets have valid DeepLOB predictions on that day. It does **not** mean all of those assets will be traded. It only means that DeepLOB produced a raw prediction for them.

This distinction is important:

```text
Signal Bus = prediction availability
Entry Mask = execution/tradability eligibility
Quality Filter = signal strength requirement
DLS Optimizer = portfolio allocation decision
```

So an asset can appear in `Signal Bus` but still be removed later because it is not tradable, has weak confidence, fails the quality filter, receives zero target weight, or does not require a rebalance trade.

---

#### 2. `Entry Mask`

`Entry Mask` is the first execution-aware gate after the raw DeepLOB prediction layer.

DeepLOB may produce a probability vector for an asset, but that does not automatically mean the asset can be traded on the execution day. The entry mask checks whether the asset is actually eligible to enter the trading/allocation pipeline.

Conceptually:

```text
Entry Mask = assets that are tradable and have valid entry information
```

In the shifted no-lookahead convention of the notebook, the signal is generated on signal day `t`, and the trade is applied on the next trade day. Therefore, the entry condition is tied to the execution day, not to future outcome information.

A simplified mathematical form is:

```text
m_i^{entry}(t) = 1
if asset i has valid tradability and valid entry price on the execution day

m_i^{entry}(t) = 0
otherwise
```

More explicitly:

```text
m_i^{entry}(t) = 1
if valid_mask_i(t+1) = 1
and entry_price_i(t+1) is finite
and entry_price_i(t+1) > 0
```

Otherwise:

```text
m_i^{entry}(t) = 0
```

This means the entry mask is not mainly a prediction-quality filter. It does not ask whether the signal is strong. Instead, it asks:

```text
Can this asset be considered for trading on the execution day?
```

The number shown in the `Entry Mask` box is the number of assets that passed this initial eligibility/tradability check.

Interpretation:

* If `Signal Bus` is much larger than `Entry Mask`, many assets had DeepLOB predictions but were not eligible for trading.
* If `Signal Bus` and `Entry Mask` are close, most assets with DeepLOB signals were tradable.
* Passing the entry mask does not mean the asset will be bought or sold. It only means the asset is allowed to move forward into the DLS allocation pipeline.

This stage is necessary because a realistic trading engine must separate **prediction availability** from **execution eligibility**.

For example, an asset can have:

```text
P(up) = 0.90
```

but if it has no valid execution price or is not tradable on the execution day, it should not receive a portfolio weight or order. The entry mask prevents this kind of unrealistic allocation.

---

#### 3. `Quality Filter`

`Quality Filter` is the signal-strength and signal-reliability gate.

After assets pass the entry mask, the notebook applies a quality filter based on DeepLOB-derived features. This stage decides whether a tradable asset has a strong enough signal to justify portfolio allocation and possible transaction costs.

The two main features are:

```text
signal_score = P(up) - P(down)

confidence = |signal_score| × (1 - P(flat))
```

The logic is:

```text
signal_score > 0
```

means the model is more bullish than bearish.

```text
confidence high
```

means the signal is both directional and not dominated by the flat/no-direction class.

In the notebook configuration, the quality filter keeps assets satisfying:

```text
confidence >= 0.02
signal_score >= 0.00
```

So the filter keeps assets that are at least non-bearish and have enough directional confidence.

This stage answers:

```text
Among the tradable assets, which ones have a strong enough DeepLOB signal to be considered by DLS?
```

This is important because trading every raw signal would create excessive turnover and transaction costs. The quality filter reduces noisy activity before the DLS optimizer assigns target weights.

The stage line usually reports:

```text
quality_candidates
pass rate = quality_candidates / entry_candidates
```

Where:

| Term                 | Meaning                                                        |
| -------------------- | -------------------------------------------------------------- |
| `entry_candidates`   | Assets that passed the entry/tradability mask.                 |
| `quality_candidates` | Entry candidates that also passed the signal-quality filter.   |
| `pass rate`          | Fraction of entry candidates that survived the quality filter. |

Interpretation:

* A high pass rate means most tradable assets had acceptable signal quality.
* A low pass rate means many tradable assets had weak, flat, or unreliable signals.
* If too few assets pass the quality filter, the notebook can fall back to top-confidence tradable assets to keep the portfolio investable and satisfy minimum-holding constraints.

This stage explains why a stock can have a positive `P(up)` but still receive no DLS allocation. A positive `P(up)` alone is not enough. The signal must also be strong relative to `P(down)`, not too flat, and pass the configured quality thresholds.

For example:

```text
P(down) = 0.20
P(flat) = 0.75
P(up)   = 0.05
```

This asset is not useful for a long-only allocation because the signal is mostly flat/uncertain and not bullish.

Another example:

```text
P(down) = 0.30
P(flat) = 0.35
P(up)   = 0.35
```

Here `P(up)` is positive, but the directional edge is very small:

```text
signal_score = 0.35 - 0.30 = 0.05
```

If the resulting confidence is too low, the asset may be filtered out.

So the quality filter is the bridge between raw DeepLOB predictions and the DLS optimizer. It makes sure that DLS receives a cleaner, more reliable candidate set instead of the full noisy signal universe.


#### 4. `DLS Optimizer`

`DLS Optimizer` is the portfolio construction stage.

After the entry and quality filters, the DLS layer converts the surviving DeepLOB signal features into target portfolio weights. In the notebook, the DLS model is a long-only allocation layer. Its output is post-processed into executable target weights.

Conceptually, this stage converts:

```text
filtered DeepLOB signal features
    -> target portfolio weights W_t
```

The target weight means:

```text
W_{t,i} = desired portfolio fraction allocated to asset i
```

Interpretation:

* If `target_weight > 0`, the DLS optimizer wants the portfolio to hold that asset.
* If `target_weight = 0`, the optimizer does not want exposure to that asset.
* Since this implementation is long-only, final executable weights are normally non-negative.
* The optimizer does not simply buy every positive DeepLOB signal. It ranks, filters, and allocates across assets under portfolio-level constraints.

The number shown in this stage is the number of assets with positive target weights, often called `target_names`.

This stage is where the system moves from prediction to allocation. It answers:

```text
Which filtered signals became actual portfolio targets?
```

A useful way to read this stage is to compare it with the previous one:

```text
quality_candidates -> target_names
```

If many assets pass the quality filter but only a smaller number receive target weights, the optimizer is concentrating capital into the most attractive subset.

---

حتماً. این نسخه را می‌توانی جای بخش‌های **Execution**, **Portfolio State** و **How to read the full stage line** در README بگذاری. متن را واضح‌تر کردم و با منطق notebook و dashboard هماهنگ‌تر نوشتم؛ مخصوصاً تفاوت `target weight`, `submitted order`, `filled execution`, و `EOD holding` را روشن‌تر کردم.

#### 5. `Execution`

`Execution` is the trading-action stage of the pipeline.

Up to this point, the system has only produced target portfolio weights. However, a target weight is not yet a real trade. The execution stage converts the DLS target portfolio into actual buy and sell actions.

The core idea is:

```text
rebalance gap = DLS target weight - current portfolio weight
```

For each asset, the engine compares the asset’s current portfolio weight with its new DLS target weight.

Examples:

```text
current weight = 2%, target weight = 5%
-> the engine needs to increase exposure
-> BUY approximately 3% more portfolio weight
```

```text
current weight = 6%, target weight = 1%
-> the engine needs to reduce exposure
-> SELL approximately 5% portfolio weight
```

```text
current weight = 3%, target weight = 0%
-> the asset is no longer desired by DLS
-> SELL or close the position
```

This means that `SELL` in this project does **not** necessarily mean that DeepLOB is bearish. It usually means:

```text
The asset was already held,
but its new DLS target weight is lower than its previous/current weight.
```

So an asset can have a high `P(up)` and still be sold if the portfolio already holds more of that asset than the new target allocation requires.

---

##### Target weight vs submitted order vs filled execution

This stage is easier to understand if these three concepts are separated:

| Concept              | Meaning                                                                                  |
| -------------------- | ---------------------------------------------------------------------------------------- |
| `DLS target weight`  | The portfolio weight the optimizer wants after rebalancing.                              |
| `Submitted order %`  | The buy/sell percentage the strategy sends to the execution engine.                      |
| `Filled execution %` | The part of the submitted order that is actually executed after constraints are applied. |

So the chain is:

```text
DLS target weight
    -> rebalance gap
    -> submitted buy/sell order
    -> filled buy/sell execution
```

A submitted order is the strategy’s intention. A filled execution is what actually happened.

Therefore:

```text
submitted order ≠ filled execution
```

For example:

```text
DLS submitted buy % = 2.0%
Filled buy %        = 1.5%
```

means the strategy attempted to buy 2.0% portfolio exposure, but only 1.5% was actually filled.

Or:

```text
DLS submitted sell % = 1.0%
Filled sell %        = 0.0%
```

means a sell order was submitted, but the trade audit did not record a filled sell execution for that asset.

---

##### Execution constraints

The notebook simulates realistic trading mechanics. Even if the target weight says the portfolio should buy or sell an asset, the execution engine may reduce, skip, or modify the trade because of practical constraints.

The main execution constraints include:

| Constraint                     | Meaning                                                                     |
| ------------------------------ | --------------------------------------------------------------------------- |
| `available cash`               | The portfolio must have enough cash to buy shares.                          |
| `cash buffer`                  | A fixed part of capital is kept as cash and not fully invested.             |
| `lot size`                     | Orders must respect minimum share-lot rules.                                |
| `sellable shares`              | The strategy can only sell shares that are available to sell.               |
| `locked shares`                | Some shares may not be sellable immediately.                                |
| `transaction costs`            | Commissions and taxes reduce net value.                                     |
| `commission`                   | Fee paid on executed trades.                                                |
| `stamp duty`                   | Additional sell-side tax/cost.                                              |
| `execution price availability` | Trades require valid execution prices.                                      |
| `rebalance band`               | Very small weight differences may be ignored to avoid unnecessary turnover. |

The execution stage therefore answers:

```text
After portfolio targets and real trading constraints, what did the strategy actually trade?
```

---

##### Buy and sell interpretation

A `BUY` action usually means:

```text
DLS target weight > previous/current weight
```

and the difference is large enough to justify a rebalance.

A `SELL` action usually means:

```text
DLS target weight < previous/current weight
```

and the position must be reduced.

A `HOLD` action usually means one of the following:

```text
target weight is close to current weight
the rebalance gap is too small
the order is blocked by constraints
the asset does not need a trade
```

The dashboard’s execution tables make this visible through columns such as:

```text
Previous weight
DLS predicted target weight
Post-trade weight
Final EOD weight
DLS submitted buy %
DLS submitted sell %
Filled buy %
Filled sell %
Filled buy shares
Filled sell shares
Buy turnover
Sell turnover
Buy cost
Sell cost
```

These columns should be read together. The action label alone is not enough to explain the trade; the weight transition explains why the action happened.

---

##### Open-based execution convention

In this notebook version, DLS execution uses an open-based trading convention. The rebalance decision is applied using execution prices available at the trading stage, while the same-day close is used later for valuation and logging.

In practical terms:

```text
open / decision price -> used for execution sizing
close price           -> used for end-of-day valuation
```

This avoids using end-of-day information to size trades that should have been decided before the close.

---

##### What the Execution stage reports

The `Execution` box in the stage line reports how many assets were actually executed on the selected replay day. It separates executed activity into buys and sells.

This is different from the number of target names.

For example:

```text
DLS Optimizer = 240 target names
Execution     = 35 executed assets
```

This means DLS wanted a portfolio with 240 positive-weight assets, but only 35 assets needed actual trading on that day. The rest may already have been close to target weight or may not have required a rebalance.

So this stage answers:

```text
What did the strategy actually trade today?
```

---

#### 6. `Portfolio State`

`Portfolio State` is the final end-of-day state of the DLS portfolio after the trading step.

After the execution engine updates cash and holdings, the notebook values the portfolio using end-of-day prices. The dashboard then shows the final holdings, weights, concentration, and portfolio state for the selected replay day.

This stage represents the result of the full chain:

```text
DeepLOB signals
    -> entry mask
    -> quality filter
    -> DLS target weights
    -> submitted orders
    -> filled executions
    -> updated holdings
    -> EOD valuation
```

The main number in the `Portfolio State` box is the number of active end-of-day holdings. In other words, it shows how many assets remain in the DLS portfolio after the day’s trades and valuation.

The subtitle usually reports top-10 concentration:

```text
top-10 concentration = sum of the 10 largest EOD portfolio weights
```

This tells how concentrated the portfolio is in its largest positions.

---

##### Important portfolio-state columns

The portfolio state is explained by the holding snapshot and related dashboard columns:

| Column                            | Meaning                                               |
| --------------------------------- | ----------------------------------------------------- |
| `shares`                          | Number of shares held at the end of the day.          |
| `close_price`                     | Closing price used for EOD valuation.                 |
| `market_value`                    | End-of-day value of the position.                     |
| `Final EOD weight` / `eod_weight` | Final portfolio weight of the asset after valuation.  |
| `EOD holdings`                    | Number of active assets with positive final holdings. |
| `Top-10 weight`                   | Total weight of the ten largest holdings.             |

The portfolio state is not just a list of trades. It is the final result after all trades have affected cash, holdings, and valuation.

---

##### How to interpret holdings and concentration

A larger number of holdings usually means the DLS portfolio is more diversified.

A smaller number of holdings usually means the portfolio is more selective or concentrated.

A high top-10 concentration means a large part of the portfolio is allocated to the ten largest positions. This can indicate stronger conviction, but it can also increase concentration risk.

A lower top-10 concentration means the portfolio is more evenly distributed across assets.

Examples:

```text
EOD holdings = 300
Top-10 weight = 12%
```

This suggests a broad portfolio with relatively low concentration.

```text
EOD holdings = 80
Top-10 weight = 45%
```

This suggests a much more concentrated portfolio.

---

##### Portfolio value and DLS Equity Proxy

The dashboard reconstructs the DLS portfolio value path from the exported holdings and target gross exposure. The raw DLS equity proxy is:

```text
dls_equity_proxy_raw = eod_market_value / engine_target_gross
```

In the latest dashboard version, the DLS equity path is normalized so that the first valid DLS replay value starts from:

```text
RMB 50,000,000
```

This makes the dashboard visually consistent with the notebook’s initial capital assumption and allows a clearer comparison against the Base DeepLOB portfolio path.

So the displayed DLS equity proxy is:

```text
dls_equity_proxy = normalized version of dls_equity_proxy_raw
```

This normalization does not change the return path shape. It only rescales the level of the DLS equity curve to start from the same initial capital.

---

##### What the Portfolio State stage answers

This stage answers:

```text
What does the portfolio look like after the day’s decisions have been executed and valued?
```

It helps diagnose whether DLS is producing:

* a diversified portfolio,
* a concentrated portfolio,
* stable holdings,
* aggressive rebalancing,
* or a portfolio that frequently changes its composition.

The final state is especially useful when compared with the previous stages. For example, a day may have many raw signals and many target names, but the final portfolio may still remain stable if most target weights are close to existing holdings.

---

### How to read the full stage line

The stage line should be interpreted as a decision funnel and state transition:

```text
Raw DeepLOB signal universe
    -> entry-tradable universe
    -> high-quality signal candidates
    -> DLS target portfolio
    -> filled trades
    -> final EOD holdings
```

Each transition explains a different part of the strategy.

| Transition                        | What it tells you                                                                              |
| --------------------------------- | ---------------------------------------------------------------------------------------------- |
| `Signal Bus -> Entry Mask`        | How many DeepLOB signal rows were actually tradable or eligible for the execution day.         |
| `Entry Mask -> Quality Filter`    | How many tradable assets had strong enough DeepLOB-derived signal quality.                     |
| `Quality Filter -> DLS Optimizer` | How selective the optimizer was when converting filtered signals into positive target weights. |
| `DLS Optimizer -> Execution`      | Whether target weights required real buy/sell trades after comparing with existing holdings.   |
| `Execution -> Portfolio State`    | How the filled trades changed final holdings, exposure, and concentration.                     |

---

#### Diagnostic examples

If `Signal Bus` is high but `Entry Mask` is low:

```text
DeepLOB produced many predictions, but many assets were not tradable or did not have valid entry data.
```

If `Entry Mask` is high but `Quality Filter` is low:

```text
Many assets were tradable, but most signals were too weak, too flat, or not sufficiently bullish.
```

If `Quality Filter` is high but `DLS Optimizer` is lower:

```text
Many assets passed the signal filter, but the optimizer selected a smaller subset for positive portfolio weights.
```

If `DLS Optimizer` is high but `Execution` is low:

```text
The target portfolio may be broad, but most current holdings were already close to target weights,
or the rebalance gap was too small to justify trading.
```

If `Execution` is high but `Portfolio State` changes only slightly:

```text
The strategy performed many small rebalance trades rather than large position changes.
```

If an asset has high `P(up)` but `Engine action = SELL`:

```text
This does not necessarily mean the model became bearish.
It usually means the asset was already overweight relative to its new DLS target weight.
```

If an asset has low `P(down)` but is sold:

```text
The sell action is probably a portfolio rebalancing action,
not a short-selling or bearish-signal action.
```

---

#### Final interpretation

The stage line is a compressed explanation of the full DeepLOB + DLS trading path.

It shows how the system moves through these questions:

```text
1. What did DeepLOB predict?
2. Which predicted assets were actually tradable?
3. Which tradable signals were strong enough?
4. Which assets received DLS portfolio weights?
5. Which target-weight changes became real trades?
6. What did the final end-of-day portfolio look like?
```

In short:

```text
Prediction does not equal allocation.
Allocation does not equal submitted order.
Submitted order does not equal filled execution.
Filled execution does not equal final EOD weight.
```

The dashboard stage line exists to make each of these differences visible day by day.

# 9. App views / tabs

## 9.1 `⚡ Engine Replay`

This is the main playback screen.

It answers:

- What happened on the selected trading day?
- How did the raw DeepLOB signal universe become a DLS portfolio decision?
- How many assets survived each filter stage?
- How does the DLS equity proxy compare with the Base DeepLOB portfolio path?

### Panels

#### `Replay Path`

A line chart comparing:

- `DLS equity proxy`
- `Base DeepLOB portfolio value`

The vertical dashed line marks the current replay day.

#### `Engine Event Log`

A terminal-style text block summarizing the current day:

| Field            | Meaning                                                    |
| ---------------- | ---------------------------------------------------------- |
| `trade_day`    | Trading day being replayed.                                |
| `signal_day`   | Signal day used to produce the current trade day decision. |
| `raw_assets`   | Number of assets with raw signal information.              |
| `buy_signals`  | Number of raw DeepLOB buy signals.                         |
| `avg_P_up`     | Average`P(up)` over the raw signal universe.             |
| `max_P_up`     | Maximum`P(up)` on the selected day.                      |
| `entry`        | Number of assets passing the entry mask.                   |
| `quality`      | Number of assets passing the quality filter.               |
| `pass_rate`    | `quality_candidates / entry_candidates`.                 |
| `target_names` | Number of assets receiving a positive DLS target weight.   |
| `target_gross` | Gross DLS target exposure.                                 |
| `max_weight`   | Largest target weight on the selected day.                 |
| `buys`         | Number of executed DLS buy trades.                         |
| `sells`        | Number of executed DLS sell trades.                        |
| `turnover`     | DLS executed turnover.                                     |
| `cost`         | DLS trading cost.                                          |
| `holdings`     | Number of EOD holdings.                                    |
| `equity_proxy` | Reconstructed DLS equity proxy.                            |
| `cash_proxy`   | Reconstructed DLS cash proxy.                              |
| `top10_weight` | Sum of the top 10 EOD holding weights.                     |

#### `Current Decision Funnel`

A funnel chart showing how assets move through the pipeline:

```text
Raw Signals -> Entry -> Quality Filter -> Target Weights -> Executed -> EOD Holdings
```

#### `DeepLOB 3-Signal Monitor`

This section shows the three DeepLOB class probabilities for the same replay day:

- `P(down)`
- `P(flat)`
- `P(up)`

It includes:

- Three signal summary cards.
- A stacked probability chart for the top selected assets.
- A combined signal-to-decision table.

### DeepLOB 3-Signal table columns

| Column                | Meaning                                                                                                            |
| --------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `asset_id`          | Asset identifier.                                                                                                  |
| `signal_label`      | Dominant signal class:`Down`, `Flat`, or `Up`.                                                               |
| `prob_down`         | DeepLOB probability of a downward move.                                                                            |
| `prob_flat`         | DeepLOB probability of no meaningful move.                                                                         |
| `prob_up`           | DeepLOB probability of an upward move.                                                                             |
| `dominant_prob`     | Maximum of`prob_down`, `prob_flat`, and `prob_up`.                                                           |
| `signal_margin`     | Difference between the largest and second-largest class probability. Higher means the prediction is more decisive. |
| `signal_score`      | DLS signal score, typically derived from bullish minus bearish information.                                        |
| `confidence`        | DLS confidence value used for filtering/ranking.                                                                   |
| `entry_candidate`   | Whether the asset passed the initial entry mask.                                                                   |
| `quality_candidate` | Whether the asset passed the quality/confidence filter.                                                            |
| `target_weight`     | DLS target portfolio weight.                                                                                       |
| `engine_action`     | Action assigned by the engine:`BUY`, `SELL`, `TARGET`, or `NO TARGET`.                                     |
| `buy_shares`        | Number of shares bought by DLS.                                                                                    |
| `sell_shares`       | Number of shares sold by DLS.                                                                                      |

---

## 9.2 `🧠 Decision Inspector`

This is the asset-level decision book.

It answers:

- Which assets had strong DeepLOB signals across all three classes: `P(down)`, `P(flat)`, and `P(up)`?
- Which assets passed the DLS entry and quality filters?
- What was the asset's previous portfolio weight before the rebalance?
- What target weight did the DLS optimizer predict?
- What was the post-trade weight and final EOD weight?
- How much was submitted as a buy/sell order, and how much was actually filled?
- How did DLS orders differ from Base DeepLOB orders?

### Main table: `Combined Decision Book`

This table is the main asset-level explanation table in the Decision Inspector. It combines the DeepLOB signal, DLS filtering result, portfolio-weight transition, submitted order, filled execution, and final holding state into one row per asset.

The purpose of this table is to make the full decision chain readable without switching between multiple CSV files. For each asset, you can see:

```text
DeepLOB probabilities
    -> DLS signal score / confidence
    -> entry and quality filter status
    -> previous weight
    -> DLS predicted target weight
    -> post-trade weight
    -> final EOD weight
    -> submitted buy/sell %
    -> filled buy/sell %
    -> filled shares and turnover
```

The most important distinction is:

- `DLS submitted buy %` / `DLS submitted sell %` describe the order that the DLS strategy submitted.
- `Filled buy %` / `Filled sell %` describe what was actually filled according to the trade-audit file.
- `Previous weight`, `DLS predicted target weight`, `Post-trade weight`, and `Final EOD weight` explain how the asset moved inside the portfolio.

| Column                          | Meaning                                                                                                                                             |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Asset`                       | Asset identifier.                                                                                                                                   |
| `Engine action`               | Final action assigned by the replay engine for the current day:`BUY`, `SELL`, or `HOLD`.                                                      |
| `DeepLOB P(down)`             | DeepLOB probability that the asset will move downward.                                                                                              |
| `DeepLOB P(flat)`             | DeepLOB probability that the asset will remain approximately flat or have no meaningful directional move.                                           |
| `DeepLOB P(up)`               | DeepLOB probability that the asset will move upward.                                                                                                |
| `Signal score`                | DLS signal score used by the allocation layer. It summarizes directional signal information used downstream by the portfolio engine.                |
| `Confidence`                  | Confidence or quality score used to rank/filter assets before allocation.                                                                           |
| `Entry passed`                | Boolean indicator showing whether the asset passed the first entry mask.                                                                            |
| `Quality passed`              | Boolean indicator showing whether the asset passed the stricter quality/confidence filter.                                                          |
| `Previous weight`             | Asset weight before the current day’s trading action. This is the starting portfolio exposure before the rebalance.                                |
| `DLS predicted target weight` | Target portfolio weight predicted/assigned by the DLS optimizer for the selected asset. This is the desired allocation before execution.            |
| `Post-trade weight`           | Asset weight immediately after the trading step is applied in the DLS weight-step audit. It shows the intended transition after execution logic.    |
| `Final EOD weight`            | Final end-of-day portfolio weight from the DLS holdings snapshot. This is the final recorded weight after the full day’s portfolio state is built. |
| `DLS submitted buy %`         | Buy percentage submitted by the`DeepLOB + DLS` strategy for this asset. This comes from the DLS submission file.                                  |
| `DLS submitted sell %`        | Sell percentage submitted by the`DeepLOB + DLS` strategy for this asset. This comes from the DLS submission file.                                 |
| `Filled buy %`                | Buy percentage that was actually filled for this asset according to the DLS trade audit.                                                            |
| `Filled sell %`               | Sell percentage that was actually filled for this asset according to the DLS trade audit.                                                           |
| `Filled buy shares`           | Number of shares actually bought by DLS for this asset.                                                                                             |
| `Filled sell shares`          | Number of shares actually sold by DLS for this asset.                                                                                               |
| `Executed turnover`           | Total executed DLS turnover for this asset, combining buy-side and sell-side filled turnover.                                                       |
| `Base submitted buy %`        | Buy percentage submitted by the Base DeepLOB strategy for the same asset and replay day.                                                            |
| `Base submitted sell %`       | Sell percentage submitted by the Base DeepLOB strategy for the same asset and replay day.                                                           |

#### How to read this table

A useful reading sequence is:

1. Start from the three DeepLOB probabilities: `DeepLOB P(down)`, `DeepLOB P(flat)`, and `DeepLOB P(up)`.
2. Check whether the asset passed `Entry passed` and `Quality passed`.
3. Compare `Previous weight` with `DLS predicted target weight`.
4. Check whether the order was submitted through `DLS submitted buy %` or `DLS submitted sell %`.
5. Check whether that order was actually filled through `Filled buy %` or `Filled sell %`.
6. Confirm the final portfolio exposure using `Final EOD weight`.

Example interpretation:

- If `DeepLOB P(up)` is high, `Quality passed` is true, and `DLS predicted target weight` is greater than `Previous weight`, the DLS engine is trying to increase exposure to that asset.
- If `DLS submitted buy %` is positive but `Filled buy %` is zero, an order was submitted but no filled buy execution is recorded in the trade-audit table.
- If `DLS predicted target weight` is positive but `Final EOD weight` remains low, the optimizer wanted exposure but the actual portfolio state did not fully reach that target.

### Controls

| Control           | Purpose                                                                 |
| ----------------- | ----------------------------------------------------------------------- |
| `Action filter` | Show all assets or only`BUY`, `SELL`, or `HOLD`.                  |
| `Sort book by`  | Sort by turnover, target weight, confidence,`P(up)`, or signal score. |
| `Rows`          | Control the number of displayed rows.                                   |
| `Quality only`  | Show only assets passing the quality filter.                            |

### Charts

#### `Signal-to-Weight Map`

Scatter plot:

- x-axis: `DeepLOB P(up)`
- y-axis: `DLS target_weight`
- point color: `engine_action`
- point size: actual engine turnover

This is useful for checking whether strong bullish signals become meaningful target weights only after filtering.

#### `Compact Weight Transition`

Grouped bar chart comparing:

- `target_weight`
- `post_trade_weight`

for the most important assets on the selected day.

### Focus Asset Explanation

The selector chooses one asset and prints a text explanation containing:

- signal values
- filter status
- pre/target/post/EOD weights
- buy/sell execution
- submitted DLS order
- submitted Base DeepLOB order

---

## 9.3 `📟 Execution Tape`

This view focuses on order execution.

It answers:

- What was actually filled?
- How large were buy and sell executions?
- Which assets were traded?
- What were the asset's `P(down)`, `P(flat)`, and `P(up)` when the order was created?
- What was the asset's previous weight, DLS target weight, post-trade weight, and final EOD weight?
- What percentage was submitted as a buy/sell order, and what percentage was actually filled?
- How did the submitted DLS order compare with the Base DeepLOB submitted order?

### Metrics

| Metric             | Meaning                        |
| ------------------ | ------------------------------ |
| `Executed buys`  | Number of DLS buy executions.  |
| `Executed sells` | Number of DLS sell executions. |
| `Turnover`       | Total DLS executed notional.   |
| `Cost`           | Total DLS trading cost.        |

### Chart: `Execution Summary by Side`

A compact chart showing buy-side versus sell-side execution turnover.

### Main table: `Execution & Order Reconciliation Table`

This table is the main execution-level audit table. It is designed to answer whether the strategy’s intended order actually became a filled trade, and how the DLS order differed from the Base DeepLOB order.

It combines four layers of information:

```text
DeepLOB probabilities
    -> DLS weight transition
    -> DLS submitted order vs Base submitted order
    -> filled DLS execution
```

This table is especially useful when an asset has a strong signal but the actual execution looks different from the target allocation. It separates:

- model signal: `DeepLOB P(down)`, `DeepLOB P(flat)`, `DeepLOB P(up)`
- allocation intent: `Previous weight`, `DLS predicted target weight`, `Post-trade weight`, `Final EOD weight`
- submitted orders: `DLS submitted buy %`, `DLS submitted sell %`, `Base net order %`
- actual fills: `Filled buy %`, `Filled sell %`, `Filled buy shares`, `Filled sell shares`

| Column                          | Meaning                                                                                                                             |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `Asset`                       | Asset identifier.                                                                                                                   |
| `Engine action`               | Action assigned by the execution view for the asset:`BUY`, `SELL`, `HOLD`, or order-related state.                            |
| `DeepLOB P(down)`             | DeepLOB probability of a downward move for the selected replay day.                                                                 |
| `DeepLOB P(flat)`             | DeepLOB probability of a flat/no-direction move for the selected replay day.                                                        |
| `DeepLOB P(up)`               | DeepLOB probability of an upward move for the selected replay day.                                                                  |
| `Confidence`                  | Signal confidence used by the DLS filtering/allocation layer.                                                                       |
| `Previous weight`             | Asset weight before execution. This is the portfolio exposure at the start of the rebalance step.                                   |
| `DLS predicted target weight` | Target weight predicted by the DLS optimizer. This is the intended allocation before execution constraints and fills are inspected. |
| `Post-trade weight`           | Weight after the DLS trade step is applied. It shows how close the asset moved toward its target after trading.                     |
| `Final EOD weight`            | Final end-of-day weight from the holdings snapshot. This is the final recorded portfolio exposure.                                  |
| `DLS submitted buy %`         | Buy percentage submitted by the DLS strategy. This is an order instruction, not necessarily a filled trade.                         |
| `DLS submitted sell %`        | Sell percentage submitted by the DLS strategy. This is an order instruction, not necessarily a filled trade.                        |
| `Filled buy %`                | Percentage of the portfolio represented by the filled DLS buy execution for this asset.                                             |
| `Filled sell %`               | Percentage of the portfolio represented by the filled DLS sell execution for this asset.                                            |
| `DLS net order %`             | DLS submitted buy percentage minus DLS submitted sell percentage. Positive means net buy; negative means net sell.                  |
| `Base net order %`            | Base DeepLOB submitted buy percentage minus Base submitted sell percentage. Positive means net buy; negative means net sell.        |
| `DLS minus Base order %`      | Difference between DLS and Base submitted net orders:`DLS net order % - Base net order %`.                                        |
| `Order overlap`               | Whether the asset was ordered by both models, only DLS, only Base, or neither.                                                      |
| `Direction case`              | Whether the two models agree or disagree directionally: both buy, both sell, opposite direction, or single-side/flat.               |
| `Filled buy shares`           | Number of shares actually bought by DLS.                                                                                            |
| `Filled sell shares`          | Number of shares actually sold by DLS.                                                                                              |
| `Buy execution price`         | Average execution price for filled buy trades.                                                                                      |
| `Sell execution price`        | Average execution price for filled sell trades.                                                                                     |
| `Buy turnover`                | Notional value of filled DLS buy trades for the asset.                                                                              |
| `Sell turnover`               | Notional value of filled DLS sell trades for the asset.                                                                             |
| `Buy cost`                    | Trading cost associated with filled buy trades.                                                                                     |
| `Sell cost`                   | Trading cost associated with filled sell trades.                                                                                    |
| `Total executed turnover`     | Total filled DLS turnover for the asset, combining buy and sell turnover.                                                           |
| `Base submitted buy %`        | Buy percentage submitted by the Base DeepLOB strategy.                                                                              |
| `Base submitted sell %`       | Sell percentage submitted by the Base DeepLOB strategy.                                                                             |

#### How to read this table

This table should be read as an execution audit:

1. Use `DeepLOB P(down)`, `DeepLOB P(flat)`, and `DeepLOB P(up)` to understand the same-day model signal.
2. Use `Previous weight` and `DLS predicted target weight` to see what the DLS optimizer wanted to change.
3. Use `DLS submitted buy %` / `DLS submitted sell %` to see what the DLS strategy ordered.
4. Use `Filled buy %` / `Filled sell %` and filled shares to see what was actually executed.
5. Use `Base net order %` and `DLS minus Base order %` to understand whether DLS behaved differently from the Base DeepLOB strategy.

Example interpretation:

- If `DLS net order %` is positive and `Base net order %` is zero, the DLS strategy submitted a buy order while the Base model did not.
- If `DLS minus Base order %` is strongly positive, DLS was more aggressive on the buy side than Base DeepLOB.
- If `DLS submitted sell %` is positive but `Filled sell %` is zero, the sell order was submitted but no sell fill appears in the execution audit.
- If `Previous weight` is higher than `DLS predicted target weight`, the optimizer is trying to reduce exposure, so a sell order may appear.
- If `Post-trade weight` and `Final EOD weight` are close to the target, the execution brought the portfolio close to the optimizer’s intended allocation.

---

## 9.4 `🧺 Portfolio State`

This view shows the end-of-day DLS portfolio state.

It answers:

- What does the DLS portfolio hold at the end of the selected day?
- Which assets dominate market value?
- What are the exact shares, prices, market values, and weights?

### Chart: `Top Holdings`

A horizontal bar chart of the largest holdings by market value. Bar labels show EOD weights.

### Table: `EOD Holding Snapshot`

| Column           | Meaning                                   |
| ---------------- | ----------------------------------------- |
| `asset_id`     | Asset identifier.                         |
| `shares`       | Shares held at the end of the day.        |
| `close_price`  | Closing price used to value the position. |
| `market_value` | End-of-day market value of the position.  |
| `eod_weight`   | End-of-day portfolio weight.              |

---

## 9.5 `📅 Daily Metrics`

This view compares Base DeepLOB and DeepLOB + DLS on the current replay day.

It is different from final model comparison: it moves with the replay slider and shows same-day metrics.

### Same-Day Scoreboard

| Card                  | Meaning                                                                         |
| --------------------- | ------------------------------------------------------------------------------- |
| `Daily Return`      | Same-day return for DLS and Base. Delta is DLS minus Base in percentage points. |
| `Cumulative Return` | Return from the first replay day to the selected day.                           |
| `Daily Cost`        | Same-day trading cost for both strategies. Lower is better.                     |
| `Holdings`          | Number of active holdings for each strategy.                                    |

### Charts

#### `Daily Return Window`

A local time window around the selected replay day, comparing daily returns for both strategies.

#### `Current-Day Delta Bar`

A compact bar chart of same-day deltas:

- daily return delta
- cumulative return delta
- holdings delta
- buy count delta
- sell count delta

Positive means DeepLOB + DLS is larger than Base DeepLOB for that metric.

#### `Trading Activity`

Grouped bar chart comparing:

- buys
- sells
- holdings

#### `Turnover and Cost`

Grouped bar chart comparing:

- turnover, shown in millions of RMB
- cost, shown in thousands of RMB

### Table: `Same-Day Metric Table`

| Column            | Meaning                                                                         |
| ----------------- | ------------------------------------------------------------------------------- |
| `Metric`        | Metric name.                                                                    |
| `DeepLOB + DLS` | Value for the DLS-enhanced strategy.                                            |
| `Base DeepLOB`  | Value for the baseline strategy.                                                |
| `Delta`         | `DeepLOB + DLS - Base DeepLOB`.                                               |
| `Better if`     | Directional interpretation: higher, lower, usually lower, or context dependent. |

Rows in this table:

| Metric row                         | Meaning                                                 |
| ---------------------------------- | ------------------------------------------------------- |
| `Daily return`                   | Same-day return.                                        |
| `Cumulative return`              | Return from the first replay day to the current day.    |
| `Portfolio value / equity proxy` | DLS reconstructed equity proxy vs Base portfolio value. |
| `Daily turnover`                 | Same-day traded notional.                               |
| `Cumulative turnover`            | Total turnover up to selected day.                      |
| `Daily trading cost`             | Same-day cost.                                          |
| `Cumulative trading cost`        | Total cost up to selected day.                          |
| `Holdings`                       | Number of active names.                                 |
| `Buy orders / executions`        | DLS executions vs Base submitted buys.                  |
| `Sell orders / executions`       | DLS executions vs Base submitted sells.                 |

---

## 9.6 `⚔ Model Comparison`

This view compares the final performance of DeepLOB + DLS against Base DeepLOB.

It answers:

- Did DLS improve total return?
- Did it improve Sharpe ratio?
- Did it reduce or worsen drawdown?
- Did it increase trading costs?
- How different were orders on the selected day?

### Final KPI cards

| Card             | Meaning                                             |
| ---------------- | --------------------------------------------------- |
| `Total Return` | Final out-of-sample total return.                   |
| `Sharpe Ratio` | Risk-adjusted return.                               |
| `Max Drawdown` | Worst peak-to-trough loss. Less negative is better. |
| `Total Costs`  | Total trading costs. Lower is better.               |

### Charts

| Chart                                     | Meaning                                                           |
| ----------------------------------------- | ----------------------------------------------------------------- |
| `Engine Race: DLS vs Base Return Path`  | Cumulative return path over the replay.                           |
| `Drawdown Replay`                       | Drawdown path for both models.                                    |
| `DeepLOB + DLS Impact vs Base DeepLOB`  | Final metric deltas: DLS minus Base.                              |
| `Largest DLS vs Base Order Differences` | Current-day assets with the largest submitted order disagreement. |

### Table: `Combined Model Difference Table`

| Column             | Meaning                                                                        |
| ------------------ | ------------------------------------------------------------------------------ |
| `asset_id`       | Asset identifier.                                                              |
| `order_overlap`  | Whether order appears in both models, DLS only, Base only, or no active order. |
| `direction_case` | Whether models agree on buy/sell direction or disagree.                        |
| `dls_net_order`  | DLS net submitted order.                                                       |
| `base_net_order` | Base DeepLOB net submitted order.                                              |
| `order_delta`    | `dls_net_order - base_net_order`.                                            |
| `dls_buy`        | DLS buy percentage.                                                            |
| `dls_sell`       | DLS sell percentage.                                                           |
| `base_buy`       | Base DeepLOB buy percentage.                                                   |
| `base_sell`      | Base DeepLOB sell percentage.                                                  |

### Expander: `Show final comparison table`

| Column               | Meaning                                                                        |
| -------------------- | ------------------------------------------------------------------------------ |
| `model`            | Model name.                                                                    |
| `total_return_%`   | Final total return in percent.                                                 |
| `cagr_%`           | Annualized growth rate.                                                        |
| `sharpe`           | Sharpe ratio.                                                                  |
| `max_drawdown_%`   | Maximum drawdown.                                                              |
| `score_proxy`      | Composite score proxy used in evaluation.                                      |
| `total_costs_RMB`  | Total trading costs in RMB.                                                    |
| `avg_turnover_RMB` | Average turnover in RMB.                                                       |
| `win_rate_%`       | Fraction of profitable days/trades, depending on how exported by the notebook. |
| `avg_holdings`     | Average number of active holdings.                                             |

---

## 9.7 `🧪 Training Lab`

This view is for model-development diagnostics rather than trading replay.

### Chart: `DLS Training Monitor`

Shows training and validation loss over epochs.

Relevant columns:

| Column                    | Meaning                                                                        |
| ------------------------- | ------------------------------------------------------------------------------ |
| `epoch`                 | Training epoch.                                                                |
| `train_loss`            | Training loss.                                                                 |
| `val_loss`              | Validation loss.                                                               |
| `best_val_loss_to_date` | Best validation loss observed up to that epoch.                                |
| `train_sharpe`          | Sharpe metric on training samples, if exported.                                |
| `val_sharpe`            | Sharpe metric on validation samples, if exported.                              |
| `train_sortino`         | Sortino metric on training samples, if exported.                               |
| `val_sortino`           | Sortino metric on validation samples, if exported.                             |
| `train_turnover`        | Training turnover diagnostic.                                                  |
| `val_turnover`          | Validation turnover diagnostic.                                                |
| `train_mean_net_return` | Mean net return on training samples.                                           |
| `val_mean_net_return`   | Mean net return on validation samples.                                         |
| `stale_epochs`          | Number of epochs since improvement, used for early-stopping style diagnostics. |

### Optional expander: `Seed search diagnostics`

Shows seed-level comparison results.

Relevant columns:

| Column               | Meaning                                |
| -------------------- | -------------------------------------- |
| `seed`             | Random seed / run identifier.          |
| `total_return_%`   | Total return achieved by that seed.    |
| `cagr_%`           | Annualized growth rate.                |
| `sharpe`           | Sharpe ratio.                          |
| `max_drawdown_%`   | Maximum drawdown.                      |
| `score_proxy`      | Composite score used for seed ranking. |
| `total_costs_RMB`  | Total costs for that seed.             |
| `avg_turnover_RMB` | Average turnover.                      |
| `win_rate_%`       | Win rate.                              |
| `avg_holdings`     | Average holdings.                      |

---

## When Does the Strategy Buy or Sell?

This section explains how buy and sell actions are generated in the DeepLOB + DLS trading engine. The most important point is that the DLS strategy does **not** buy or sell directly from `P(up)` or `P(down)`. DeepLOB first produces probabilities, DLS converts those probabilities into target portfolio weights, and the execution engine then compares the target weights with the current portfolio holdings.

The decision chain is:

```text
DeepLOB probabilities
    -> signal_score and confidence
    -> entry mask
    -> quality filter
    -> DLS target weights
    -> rebalance gap
    -> submitted buy/sell orders
    -> filled executions
    -> updated EOD portfolio
```

So in this project:

```text
DeepLOB predicts direction.
DLS decides target allocation.
The execution engine decides the actual buy/sell trades.
```

---

### 1. DeepLOB probabilities are not direct orders

For each asset, DeepLOB produces a three-class probability vector:

```text
P(down), P(flat), P(up)
```

The notebook then derives:

```text
signal_score = P(up) - P(down)

confidence = |signal_score| × (1 - P(flat))
```

These values describe the signal quality and direction. However, they are **not** themselves trading orders.

For example:

```text
P(up) = 0.95
```

does not automatically mean the strategy will buy the asset.

Similarly:

```text
P(down) = 0.90
```

does not automatically mean the DLS strategy will short the asset.

The DLS strategy is long-only in this implementation. It produces non-negative target weights:

```text
target_weight_i >= 0
```

Therefore, a `SELL` action means reducing or closing an existing long position. It does **not** mean opening a short position.

---

### 2. The quality filter controls allocation eligibility, not sell permission

The quality filter is mainly an entry/allocation filter. It determines which tradable assets are eligible to receive new or increased DLS target weights.

The configured signal-quality logic is based on:

```text
confidence >= threshold
signal_score >= threshold
```

In the notebook configuration, this is:

```text
confidence >= 0.02
signal_score >= 0.00
```

This means the quality filter keeps assets that are at least non-bearish and have enough directional confidence.

However, the quality filter should not be interpreted as a restriction on selling existing positions.

A useful rule is:

```text
BUY decisions require signal quality.
SELL decisions require an existing position and a lower target weight.
```

For example, this situation is valid:

```text
quality_candidate = False
previous_weight > 0
target_weight = 0
Engine action = SELL
```

This means the asset is no longer selected for allocation, so the existing position can be reduced or closed. The sell action is a portfolio deallocation/rebalancing decision, not necessarily a bearish DeepLOB prediction.

---

### 3. DLS target weights are the bridge between signals and trades

After entry and quality filtering, the DLS model produces a target portfolio weight for each selected asset.

Conceptually:

```text
DLS target weight = desired portfolio exposure after rebalancing
```

An asset can receive:

```text
target_weight > 0
```

if DLS wants to hold it in the portfolio.

An asset can receive:

```text
target_weight = 0
```

if DLS does not want to hold it.

The execution engine does not simply buy every positive signal. Instead, it compares the new target weight with the current portfolio weight.

The key quantity is:

```text
rebalance gap = target_weight - current_weight
```

This gap determines whether the portfolio needs to buy, sell, or hold.

---

### 4. When does a BUY happen?

A `BUY` happens when the DLS target weight is higher than the current portfolio weight by more than the rebalance band.

The simplified rule is:

```text
BUY if:

target_weight_i - current_weight_i > rebalance_band
```

In words:

```text
The strategy buys asset i when DLS wants a larger exposure than the portfolio currently holds.
```

Example:

```text
current_weight = 2.0%
target_weight  = 5.0%
rebalance gap  = +3.0%
```

The portfolio is underweight relative to the DLS target, so the execution engine attempts to buy more exposure.

A buy can also happen when the asset is not currently held:

```text
current_weight = 0.0%
target_weight  = 1.5%
```

In this case, the strategy is opening a new long position.

---

### 5. Additional BUY constraints

Even if the target weight is higher than the current weight, a buy order may be reduced or skipped because the execution engine applies realistic trading constraints.

The main buy-side constraints are:

| Constraint | Meaning |
| ---------- | ------- |
| `entry mask` | The asset must be tradable and have valid entry information. |
| `quality filter` | The signal must be strong enough to receive allocation. |
| `rebalance band` | Very small weight gaps are ignored to reduce unnecessary turnover. |
| `available cash` | The portfolio must have enough cash to buy shares. |
| `cash buffer` | A fixed part of the portfolio is kept as cash. |
| `lot size` | The order must respect minimum share-lot rules. |
| `minimum target weight` | Very small target weights may be ignored. |
| `valid buy price` | The execution price must be finite and positive. |
| `transaction costs` | Commission and costs reduce available buying power. |

So the practical buy logic is closer to:

```text
BUY if:
    target_weight_i > current_weight_i + rebalance_band
    and asset passes entry / allocation requirements
    and target_weight_i is large enough
    and valid execution price exists
    and enough cash is available
    and the final share quantity respects lot size
```

A positive DeepLOB signal does not guarantee a buy. The asset must survive the allocation and execution pipeline.

---

### 6. When does a SELL happen?

A `SELL` happens when the current portfolio weight is higher than the new DLS target weight by more than the rebalance band.

The simplified rule is:

```text
SELL if:

current_weight_i - target_weight_i > rebalance_band
```

In words:

```text
The strategy sells asset i when the portfolio currently holds more of the asset than DLS now wants.
```

Example:

```text
current_weight = 6.0%
target_weight  = 1.0%
rebalance gap  = -5.0%
```

The portfolio is overweight relative to the DLS target, so the execution engine attempts to sell part of the position.

Another example:

```text
current_weight = 3.0%
target_weight  = 0.0%
```

This means DLS no longer wants the asset in the target portfolio. The execution engine may sell or close the position.

---

### 7. Additional SELL constraints

A sell order may also be reduced or skipped because of realistic trading constraints.

The main sell-side constraints are:

| Constraint | Meaning |
| ---------- | ------- |
| `existing holdings` | The asset must already be held. |
| `sellable shares` | Only shares that are available to sell can be sold. |
| `locked shares` | Some recently bought shares may not be sellable immediately. |
| `rebalance band` | Very small overweight gaps are ignored. |
| `lot size` | Sell quantity must respect minimum share-lot rules. |
| `minimum holdings` | The engine may avoid fully exiting too many names if minimum holdings would be violated. |
| `valid sell price` | The sell execution price must be finite and positive. |
| `transaction costs` | Commission and stamp duty are charged on executed sells. |

So the practical sell logic is closer to:

```text
SELL if:
    current_weight_i > target_weight_i + rebalance_band
    and the asset is currently held
    and sellable shares are available
    and valid sell price exists
    and the final share quantity respects lot size
    and minimum-holding constraints are not violated
```

The important distinction is that selling is based on reducing an existing long exposure. It does not require `P(down)` to be high.

---

### 8. Why can an asset with high `P(up)` still be sold?

This can happen and it is not necessarily a bug.

Suppose DeepLOB is very bullish:

```text
P(down) = 0.01
P(flat) = 0.04
P(up)   = 0.95
```

But the portfolio already holds too much of this asset:

```text
current_weight = 5.0%
target_weight  = 2.0%
```

Then:

```text
current_weight - target_weight = 3.0%
```

If this is larger than the rebalance band, the execution engine may sell part of the position.

This does not mean DeepLOB became bearish. It means:

```text
The asset is still attractive, but the current position is too large relative to the new DLS target weight.
```

So:

```text
High P(up) can coexist with SELL.
```

The correct interpretation is:

```text
P(up) explains the signal.
target_weight explains the desired allocation.
current_weight vs target_weight explains the trade action.
```

---

### 9. Why can an asset with low `P(down)` still be sold?

A low `P(down)` only means the model does not strongly expect a downward move. It does not mean the portfolio must continue holding the same amount of that asset.

An asset can be sold even with low `P(down)` if:

```text
current_weight > target_weight
```

This can happen because:

```text
DLS reduced the asset's target weight.
Other assets became more attractive.
The asset failed the current quality/allocation filter.
The portfolio needs rebalancing.
The strategy needs to free cash.
The current position became overweight after price movement.
```

Therefore:

```text
Low P(down) does not block SELL.
```

Sell is primarily a portfolio-weight adjustment.

---

### 10. Why does the engine sell before buying?

In the replay engine, sell actions are processed before buy actions.

The reason is practical:

```text
Sells free cash.
Then buys use the updated cash balance.
```

The daily sequence is:

```text
1. Load current holdings and cash.
2. Load DLS target weights for the selected trade day.
3. Compute current weights using the decision/execution price.
4. Identify overweight positions:
       current_weight > target_weight + rebalance_band
5. Execute sells first.
6. Update cash and holdings.
7. Recompute current weights after sells.
8. Identify underweight positions:
       target_weight > current_weight + rebalance_band
9. Execute buys using available cash after cash buffer.
10. Update holdings, cash, costs, and audit tables.
11. Value the final portfolio at end-of-day close.
```

This order makes the execution more realistic because the strategy does not assume unlimited cash before selling overweight positions.

---

### 11. Submitted order vs filled execution

The dashboard separates the order the strategy wanted from the trade that actually happened.

```text
Submitted order = intended buy/sell percentage
Filled execution = actual trade after constraints
```

For example:

```text
DLS submitted buy % = 2.0%
Filled buy %        = 1.4%
```

This means the strategy wanted to buy 2.0% exposure, but only 1.4% was actually executed.

Another example:

```text
DLS submitted sell % = 1.0%
Filled sell %        = 0.0%
```

This means the strategy attempted to sell, but the trade was not filled because of sellability, lot size, price, or other execution constraints.

Therefore:

```text
Target weight does not always become a submitted order.
Submitted order does not always become a filled execution.
Filled execution does not always exactly match the target weight.
```

---

### 12. Practical interpretation in the dashboard

When inspecting one asset in the dashboard, the action should be explained by reading these columns together:

```text
P(down)
P(flat)
P(up)
signal_score
confidence
entry_candidate
quality_candidate
Previous weight
DLS predicted target weight
Post-trade weight
Final EOD weight
DLS submitted buy %
DLS submitted sell %
Filled buy %
Filled sell %
Filled buy shares
Filled sell shares
Buy turnover
Sell turnover
Buy cost
Sell cost
Engine action
```

The most important diagnostic comparison is:

```text
Previous weight vs DLS predicted target weight
```

Interpretation:

```text
target_weight > previous_weight
    -> buy / increase exposure

target_weight < previous_weight
    -> sell / reduce exposure

target_weight approximately equal to previous_weight
    -> hold / no meaningful rebalance
```

The DeepLOB probabilities explain why an asset was considered by the model. The weight transition explains why the execution engine bought or sold it.

---

### 13. DLS vs Original DeepLOB-only baseline

The buy/sell logic of the DLS strategy is different from the original DeepLOB-only baseline.

In the original DeepLOB-only baseline, orders are more directly tied to classification signals:

```text
High P(up) / bullish signal
    -> buy candidate

High P(down) / bearish signal
    -> sell candidate

Flat signal
    -> partial reduction or no aggressive buy
```

In the DeepLOB + DLS strategy, the process is different:

```text
DeepLOB probabilities
    -> DLS target weights
    -> current weight vs target weight
    -> buy/sell execution
```

So:

```text
Original DeepLOB-only:
    buy/sell is probability-signal driven.

DeepLOB + DLS:
    buy/sell is target-weight and rebalance-gap driven.
```

This is why DLS can sell an asset even when `P(up)` is high, and it can hold or buy an asset even when the signal is only moderately bullish, as long as the final portfolio optimization assigns it a positive target weight.

---

### 14. Final rule of thumb

The simplest way to understand the DLS execution logic is:

```text
BUY:
DLS wants more exposure than the portfolio currently has.

SELL:
DLS wants less exposure than the portfolio currently has.

HOLD:
The current exposure is already close enough to the DLS target,
or execution constraints prevent a trade.
```

More formally:

```text
BUY if:
target_weight_i - current_weight_i > rebalance_band
```

```text
SELL if:
current_weight_i - target_weight_i > rebalance_band
```

```text
HOLD if:
|target_weight_i - current_weight_i| <= rebalance_band
```

subject to:

```text
cash, lot size, sellability, locked shares, transaction costs,
minimum holdings, valid prices, and execution constraints.
```

In short:

```text
Prediction does not equal trade.
Target weight does not equal filled execution.
BUY/SELL is decided by the gap between current weight and DLS target weight.


## 10. Understanding the two strategies

### Base DeepLOB

The baseline strategy uses the original DeepLOB out-of-sample signal and submitted-order logic. Its daily portfolio path is read directly from:

```text
T001_original_deeplob_exact_daily_log.csv
```

Main base columns used:

| Column              | Meaning                             |
| ------------------- | ----------------------------------- |
| `portfolio_value` | Base portfolio value at end of day. |
| `cash`            | Base cash balance.                  |
| `num_holdings`    | Number of base holdings.            |
| `num_buys`        | Number of base buy orders.          |
| `num_sells`       | Number of base sell orders.         |
| `buy_turnover`    | Base buy notional.                  |
| `sell_turnover`   | Base sell notional.                 |
| `total_costs`     | Base trading costs.                 |

### DeepLOB + DLS

The DLS strategy starts from DeepLOB signals, applies additional filtering, generates target weights, submits orders, records filled trades, and stores end-of-day holdings.

DLS daily equity is reconstructed as:

```text
dls_equity_proxy = eod_market_value / engine_target_gross
```

This is a proxy because the exported DLS files store holdings/trades/weights rather than a single official DLS daily portfolio log.

---

## 11. Performance and reliability notes

- The app uses `st.cache_data` for CSV loading and daily summary construction.
- Only the selected Engine View is rendered.
- During auto-play, Fast Playback Mode skips heavy charts and large tables.
- Plotly charts are explicitly styled with a dark theme to avoid white-on-white rendering problems.
- If the app appears stuck, clear Streamlit cache and restart.

---

## 12. Troubleshooting

### App spins forever or becomes slow

Try:

```bash
streamlit cache clear
```

Then restart:

```bash
streamlit run streamlit_app.py
```

Also keep `Enable auto-play module` off until the app fully loads.

### Auto-play does not work

Install:

```bash
pip install streamlit-autorefresh
```

Manual replay controls still work without this package.

### Data not found

Make sure the CSVs are inside:

```text
./data/
```

relative to `streamlit_app.py`.

### Some tables are empty

That usually means the current replay day has no rows in that specific CSV export. Move the replay slider to another day or confirm the relevant CSV exists.

---

## 13. Important interpretation note

The Base DeepLOB portfolio value is read directly from the base daily log.

The DeepLOB + DLS portfolio value shown in the app is a reconstructed equity proxy:

```text
DLS equity proxy = EOD market value / target gross
```

Therefore, when comparing DLS and Base daily values, interpret the DLS path as a replay-consistent reconstructed path rather than a raw single-column portfolio log.

---

## 14. Suggested GitHub description

> Event-driven Streamlit replay dashboard for inspecting an out-of-sample DeepLOB + DLS trading pipeline, including same-day DeepLOB signal probabilities, DLS target weights, executed trades, EOD holdings, and daily/final comparison against a Base DeepLOB strategy.

---

## 15. Latest dashboard table update

The current dashboard version expands the two main asset-level audit tables:

- `Combined Decision Book`
- `Execution & Order Reconciliation Table`

Both tables now show the complete DeepLOB three-class probability vector:

```text
DeepLOB P(down), DeepLOB P(flat), DeepLOB P(up)
```

They also make the portfolio-weight transition explicit:

```text
Previous weight
    -> DLS predicted target weight
    -> Post-trade weight
    -> Final EOD weight
```

Finally, they separate submitted orders from filled executions:

```text
DLS submitted buy/sell %
    vs
Filled buy/sell %
```

This distinction is important because a model can submit an order without receiving a filled execution, and a target weight can differ from the final EOD weight due to execution, existing holdings, or portfolio-state reconstruction.
